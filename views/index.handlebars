<h1>Log-a-Burger</h1>
<img src="../public/assets/img/burger.png" alt="Burger" style="height: 300px, width: 500px"/>
<h2>Burgers to Eat</h2>

<ul>
    {{#each burgers}}
      {{#unless devoured}}
        {{!-- {{> burgers/burger-block devoured=false}} --}}
      {{/unless}}
    {{/each}}
</ul>

<h2>Devoured Burgers</h2>

<ul>
    {{#each burgers}}
      {{#if devoured}}
        {{!-- {{> burgers/burger-block devoured=true}} --}}
      {{/if}}
    {{/each}}
</ul>

<h2>Eat a Burger</h2>
<form class="devour-form">
    <div class ="form-group">
        <label for="burger">Burger Type:</label>
        <input type="text" id="burger" name="name">
    </div>
        <div class ="form-group">
        <label for="burger">Burger Type:</label>
        <input type="radio" name="devoured" value="1" checked>Devoured!<br>
        <input type="radio", name="devoured" value="0">Available 
    </div>
<button type="submit">Devour a Burger</button>
</form>